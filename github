git clone https://github.com/vnagidi/angular.git

git status

git add filename   # git add folderName

git commit -m 'first commit' filename   # git commit -am 'message'

git push origin branchName

git pull origin branchName